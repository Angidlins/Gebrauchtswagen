@model IEnumerable<Gebrauchtswagen.Models.Rechnungfahrzeug>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Menge)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PreisBeiRechnungserstellung)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fahrzeug)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Menge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreisBeiRechnungserstellung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fahrzeug.Bezeichnung)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RechnungfahrzeugId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RechnungfahrzeugId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RechnungfahrzeugId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
